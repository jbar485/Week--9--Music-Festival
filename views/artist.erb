<h2>Artist: <%= @artist.name %></h2>
<h3>Album: <%= @artist.stages.name %></h3>

<form action="/stages/<%= @artist.stages.id %>/artists/<%= @artist.id %>" method="post">
  <input name="_method" type="hidden" value="patch">

  <label for="name">Rename artist</label>
  <input id="name" name="name" type="text">

  <button type="submit">Update artist</button>
</form>

<form action="/stages/<%= @artist.stages.id %>/artists/<%= @artist.id %>" method="post">
  <input name="_method" type="hidden" value="delete">

  <button type="submit">Delete artist</button>
</form>

<a href="/stages/<%= @artist.stages.id %>">Back to <%= @artist.stages.name %></a>
